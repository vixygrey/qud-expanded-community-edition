# Revisions to skip when running git blame — mechanical, content-free changes only.
#
# Enable once per clone:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub applies this file automatically in its blame view.
#
# NOTE: because this repo squash-merges, the SHA to list is the *squash commit on main*, not the
# commit on the feature branch — the branch SHA does not survive the merge. That means an entry
# can only be added AFTER its PR merges, in a follow-up.

# style: normalise mod files to LF line endings (#32)
81070b419ec400ca1ce86b7a8be3bce6b44fffec

# style: apply ruff format to tools/ (#74)
4adf089dc65c64b2d44518075fb0a50f15000be5

# style: reformat all mod XML with prettier (#78)
d6d4c1cd5bbdd29a2ccb4058d60a802ab4c25468
