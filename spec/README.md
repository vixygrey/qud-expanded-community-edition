# Specifications

Store project specifications in this directory.

Use lowercase kebab-case names for specification files.

Describe observable behavior, prerequisites, safety rules, and failure behavior.

Use `MUST` for required behavior and `MAY` for optional behavior.

Link related scripts and configuration files by repository-relative path.

Use `template.md` as the starting structure for a new specification.
